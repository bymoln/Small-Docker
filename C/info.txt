# Run and show
docker images
docker run --name hw-c -it bymoln/helloworld-c
docker ps -a
docker start -ai hw-c

# Push to Docker Hub
docker login
Login with your Docker ID to push and pull images from Docker Hub. If you don't have a Docker ID, head over to https://hub.docker.com to create one.
Username: bymoln
Password: 

docker push bymoln/helloworld-c

